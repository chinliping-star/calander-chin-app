# React Rules

- React 19 function components only.
- Hooks at top level; no conditional hooks.
- Co-locate component + styles.
- Keep components small and composable.
- Use Tailwind v4 utilities; design tokens via CSS, not JS config.
